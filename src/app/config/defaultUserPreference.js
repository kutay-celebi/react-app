import {en, PATH_LOGIN_ROOT} from "../constants";

const defaultUserPreference = {
    layout    : {
        style : "layout1",
        config: {}
    },
    language  : en,
    defaultUrl: PATH_LOGIN_ROOT
};

export default defaultUserPreference;